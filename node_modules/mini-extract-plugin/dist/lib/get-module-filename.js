"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function getModuleFilename(options) {
    const { default: defaultFilename, context, templateOptions, modules, } = options;
    const { plugin } = context;
    const moduleFilename = plugin.options.moduleFilename;
    if (!moduleFilename)
        return defaultFilename;
    if (typeof moduleFilename === 'string')
        return moduleFilename;
    return moduleFilename(context, templateOptions, modules);
}
exports.default = getModuleFilename;
