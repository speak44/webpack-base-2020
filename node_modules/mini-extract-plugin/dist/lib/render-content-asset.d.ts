import { Source } from 'webpack-sources';
import type { RenderContext } from '../types/context';
import type { MiniExtractPlugin } from '../types/subclassing';
import type { GetModule } from '../types/subclassing-util';
export declare type RenderContentAssetOptions<MEP extends MiniExtractPlugin = MiniExtractPlugin> = {
    context: RenderContext<MEP>;
    modules: GetModule<MEP>[];
    ignoreOrder: boolean;
};
export default function renderContentAsset<MEP extends MiniExtractPlugin = MiniExtractPlugin>({ context, modules, ignoreOrder }: RenderContentAssetOptions<MEP>): Source;
