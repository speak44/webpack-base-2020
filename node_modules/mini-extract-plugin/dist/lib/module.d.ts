import type { Module } from '../types/subclassing';
export declare function findById<T extends Module>(modules: T[], id: string): T | null;
export declare function evalCode(loaderContext: any, code: string, filename: string): any;
